[package]
name = "decryption-types"
version = "0.1.0"
edition = "2021"

[dependencies]
witness-types = { path = "../witness-types" }
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
