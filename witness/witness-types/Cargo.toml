[package]
name = "witness-types"
version = "0.1.0"
edition = "2021"

[dependencies]
pir-types = { path = "../../shared/pir-types" }
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
