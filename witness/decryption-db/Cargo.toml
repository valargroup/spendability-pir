[package]
name = "decryption-db"
version = "0.1.0"
edition = "2021"

[dependencies]
decryption-types = { path = "../decryption-types" }
xxhash-rust = { version = "0.8", features = ["xxh64"] }
thiserror = "2"
